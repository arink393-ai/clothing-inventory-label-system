import { PageHead } from "@/components/page-head";
export function WorkflowPage({title,description,action,columns,empty}:{title:string;description:string;action:string;columns:string[];empty:string}){return <><PageHead title={title} description={description}><button className="btn" disabled>匯出 Excel（開發中）</button><button className="btn primary" disabled>＋ {action}（開發中）</button></PageHead><section className="panel"><div className="notice page-notice">此功能將在下一個開發階段接上 Supabase；目前按鈕已停用，避免誤操作。</div><div className="table-wrap"><table><thead><tr>{columns.map(c=><th key={c}>{c}</th>)}</tr></thead><tbody><tr><td colSpan={columns.length} className="empty">{empty}</td></tr></tbody></table></div></section></>}
